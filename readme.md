connects the meta neural band to my mac by copying the pairing process from the app

NO sEMG/GESTURE DATA CAN BE READ, JUST DOES THE INITIAL PAIRING PROCESS (i.e. it is useless)


sniffing:

packetlogger, install profile on ios then connect to mac, export logs to btsnoop

wireshark to view btsnoop logs

run meta_band_edch.py to pair and you'll see the entry for the meta band in mac bt settings (again, does nothing)
